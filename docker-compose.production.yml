services:
  db:
    container_name: foodgram-db
    image: postgres:13.0-alpine
    restart: always
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - .env  # путь изменён на локальный

  backend:
    container_name: foodgram-app
    build: ./backend  # путь изменён на локальный
    restart: always
    volumes:
      - static_dir:/app/static/
      - media_dir:/app/media/
    env_file:
      - .env  # путь изменён на локальный
    depends_on:
      - db

  frontend:
    container_name: foodgram-front
    build: ./frontend  # путь изменён на локальный
    volumes:
      - ./frontend/:/app/result_build/

  nginx:
    container_name: foodgram-proxy
    image: nginx:1.23.3-alpine
    restart: always
    ports:
      - 80:80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./frontend/build:/usr/share/nginx/html/
      - ./docs/:/usr/share/nginx/html/api/docs/
      - static_dir:/etc/nginx/html/static/
      - media_dir:/etc/nginx/html/media/
    depends_on:
      - backend
      - frontend

volumes:
  static_dir:
  media_dir:
  postgres_data:

# # docker-compose.yml
# version: '3.3'

# volumes:
#   pg_data:
#   static:
#   media:

# services:
#   db:
#     image: postgres:13
#     env_file: .env
#     volumes:
#       - pg_data:/var/lib/postgresql/data

#   backend:
#     build: ./backend
#     env_file: .env
#     volumes:
#       - static:/app/static
#       - media:/app/media
#     depends_on:
#       - db

#   frontend:
#     container_name: foodgram-front
#     build: ../frontend
#     volumes:
#       - ../frontend:/app/source
#       - /app/build

#   nginx:
#     container_name: foodgram-proxy
#     image: nginx:1.25.4-alpine
#     depends_on:
#       - backend
#       - frontend
#     ports:
#       - "80:80"
#     volumes:
#       - ./nginx.conf:/etc/nginx/conf.d/default.conf
#       - ../frontend/build:/usr/share/nginx/html
#       - ../docs:/usr/share/nginx/html/api/docs
#       - static:/usr/share/nginx/html/static
#       - media:/usr/share/nginx/html/media